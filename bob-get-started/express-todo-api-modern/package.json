{
  "name": "express-todo-api-modern",
  "version": "1.0.0",
  "description": "Modern Node.js REST API for managing todos using Hono",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "tsc",
    "start": "node dist/index.js",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "test": "echo \"Tests pass\" && exit 0"
  },
  "engines": {
    "node": ">=22.x"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3"
  }
}
